<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.capres">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.CaPres">
        <activity android:name=".ui.user.UserActivity"></activity>
        <activity android:name=".ui.user.UserDetailActivity" />
        <activity android:name=".ui.splash.SplashActivity" />
        <activity android:name=".ui.login.LoginActivity" />
        <activity android:name=".ui.admin.AdminActivity" />
        <activity android:name=".ui.admin.AdminDetailActivity" />
        <activity android:name=".adapter.AdminAdapter" />
        <activity android:name=".adapter.UserAdapter" />
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>